const str1 = "React";
const str2 = "Elon Musk";
const str3 = "Apple founder is Stew Job";

function firstChar(len){
  return len.charAt(0)
}
//const len = str1.charAt(0);
console.log(`first character of the "${str1}" string is ${firstChar(str1)}`);
console.log(`First character of the "${str2}" string is ${firstChar(str2)}`);
console.log(`First character of the "${str3}" string is ${firstChar(str3)}`);